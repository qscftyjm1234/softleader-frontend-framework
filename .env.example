# =================================================================
# 專案環境變數範例檔 (.env.example)
# =================================================================
# 使用說明:
# 1. 複製此文件並重新命名為 `.env`
# 2. 根據開發需求修改內容
# =================================================================

# [API 設定]
# API 的基礎路徑 (例如: /api)
VITE_API_BASE_URL=/api
# 開發伺服器代理的目標後端地址
NUXT_PROXY_TARGET=http://localhost:8080
# API 超時時間 (毫秒)
VITE_API_TIMEOUT=10000

# [Mock API 功能]
# 是否啟用 Mock API 功能 (true = 啟用假資料, false = 連接真實後端)
NUXT_PUBLIC_FEATURE_API_MOCK=true
# Mock API 的回應延遲時間 (毫秒)
NUXT_PUBLIC_MOCK_DELAY=500

# [細粒度 Mock 控制] (選填)
# 格式為 NUXT_PUBLIC_MOCK_{MODULE_NAME}_API
# NUXT_PUBLIC_MOCK_DASHBOARD_API=true
# NUXT_PUBLIC_MOCK_USER_API=false

# [資安模式]
# 是否啟用資安防護 (如禁用右鍵、F12 等，預設建議開發環境關閉)
NUXT_PUBLIC_ENABLE_SECURITY_MODE=false

# [安全性設定]

