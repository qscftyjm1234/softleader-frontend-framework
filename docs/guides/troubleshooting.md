# 問題診斷與解決方案 (Troubleshooting)

## 問題分析

你遇到的問題：

1. ❌ **沒有 ESLint 警告顯示**
2. ❌ **儲存時沒有自動格式化**

### 根本原因

當你執行 `Remove-Item -Recurse -Force node_modules` 後，所有的 npm 套件都被刪除了，包括：

- `eslint`
- `prettier`
- `@nuxt/eslint`
- 所有其他依賴

因此 VS Code 的 ESLint 和 Prettier 擴充套件找不到這些工具，無法運作。

---

## 解決方案

### 方案 1: 使用 --legacy-peer-deps（推薦）

```bash
# 目前正在執行中...
npm install --legacy-peer-deps
```

這個命令會繞過 Storybook 的相依性衝突問題。

### 方案 2: 暫時移除 Storybook

如果方案 1 失敗，編輯 `package.json`，暫時移除：

```json
{
  "devDependencies": {
    // 暫時註解或刪除這兩行
    // "@storybook/test": "^8.6.14",
    // "@storybook/vue3-vite": "^10.1.10",
  }
}
```

然後執行：

```bash
npm install
```

### 方案 3: 使用 pnpm（最穩定）

```bash
# 安裝 pnpm
npm install -g pnpm

# 使用 pnpm 安裝
pnpm install
```

pnpm 對相依性衝突的處理比 npm 更好。

---

## 安裝完成後的驗證步驟

### 1. 重新載入 VS Code ESLint 伺服器

1. 按 `Ctrl+Shift+P`
2. 輸入 "ESLint: Restart ESLint Server"
3. 執行

### 2. 重新載入 VS Code 視窗

1. 按 `Ctrl+Shift+P`
2. 輸入 "Developer: Reload Window"
3. 執行

### 3. 測試 ESLint

打開 `docs/integration-test.vue`，應該看到：

- 🔴 紅色波浪線在缺少 JSDoc 的函式上
- 🟡 黃色波浪線在未使用的變數上

### 4. 測試 Prettier

在 `docs/integration-test.vue` 中：

1. 故意打亂格式：`const x=[1,2,3]`
2. 按 `Ctrl+S` 儲存
3. 應該自動格式化成：`const x = [1, 2, 3]`

---

## 目前狀態

- ⏳ **npm install --legacy-peer-deps** 正在執行中
- ⏳ 等待安裝完成...
- ⏳ 完成後需要重新載入 VS Code

---

## 為什麼會有相依性衝突？

你的 `package.json` 中有：

```json
{
  "@storybook/test": "^8.6.14", // 版本 8
  "@storybook/vue3-vite": "^10.1.10" // 版本 10
}
```

這兩個套件的版本不一致，導致 npm 無法解析相依性。

### 長期解決方案

統一 Storybook 版本：

```json
{
  "@storybook/test": "^10.1.10", // 改成版本 10
  "@storybook/vue3-vite": "^10.1.10" // 保持版本 10
}
```

或者全部改成版本 8：

```json
{
  "@storybook/test": "^8.6.14", // 保持版本 8
  "@storybook/vue3-vite": "^8.6.14" // 改成版本 8
}
```

---

## 下一步

1. ⏳ 等待 `npm install --legacy-peer-deps` 完成
2. ✅ 重新載入 VS Code ESLint 伺服器
3. ✅ 測試 ESLint 和 Prettier
4. ✅ 如果還是有問題，嘗試方案 2 或 3
